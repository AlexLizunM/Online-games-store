!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title OnlineGamesStore
    = csrf_meta_tags
    = csp_meta_tag
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track': 'reload'
  %body
    %nav.navbar-dark.bg-dark
      %span
        .navbar-brand.mb-0.h1 Online Game Store
        .row.links.justify-content-end
          .col-2
            .row.justify-content-end
              - if user_signed_in?
                = link_to "Log out", destroy_user_session_path, method: :delete, class: 'btn btn-primary'
                .dropdown
                  %button#dropdownMenuButton.btn.btn-secondary.dd-bg.dropdown-toggle{"aria-expanded" => "false", "aria-haspopup" => "true", "data-toggle" => "dropdown", :type => "button"}
                  .dropdown-menu.dropdown-menu-right{"aria-labelledby" => "dropdownMenuButton"}
                    %a.dropdown-item{:href => 'users/user', user: current_user} Your profile
                    .dropdown-divider
                    %a.dropdown-item{:href => 'users/sign_out', 'data-method' => 'delete'} Log out
              - else
                = link_to 'Sign up', new_user_registration_path, method: :get, class: 'btn btn-primary mr-2'
                = link_to 'Log in', new_user_session_path, class: 'btn btn-primary'

    - flash.each do |key, value|
      = content_tag :div, value, class: "alert #{flash_class(key)}"
    = yield 
